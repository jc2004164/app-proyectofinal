import { ShortDireccionPipe } from './short-direccion.pipe';

describe('ShortDireccionPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortDireccionPipe();
    expect(pipe).toBeTruthy();
  });
});
